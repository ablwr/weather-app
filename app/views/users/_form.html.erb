<br>
<%= form_for(@user) do |f| %>
  <% if @user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@user.errors.count, "error") %> prohibited this user from being saved:</h2>

      <ul>
      <% @user.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <br><br><br><br><br><br><br><br><br>
  <div class="field">
    <%= f.text_field :name, size: "16x15", placeholder: "Name" %>
    <%= f.email_field :email, size: "16x15", placeholder: "Email"  %>
    <%= f.time_field :time, size: "16x12", placeholder: "Time"  %>
    <%= time_zone_select( "user", "time_zone", ActiveSupport::TimeZone.us_zones, default: "Eastern Time (US & Canada)" ) %>
    <%= f.telephone_field :phone, size: "16x15", placeholder: "Telephone"  %>
    <%= f.text_field :address, size: "16x15", placeholder: "Address"  %>
  </div>
  <br><br><br><br><br>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
